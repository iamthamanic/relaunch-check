# Optional. The dashboard binds to localhost only by default.
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=4173

# Later integrations. Do not commit real secrets.
ERP_API_BASE_URL=
ERP_API_TOKEN=
GSC_SERVICE_ACCOUNT_JSON=
